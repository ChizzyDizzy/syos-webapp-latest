# Database Configuration for SYOS POS System
# Supports both CLI and Web Application

# Database Connection
db.url=jdbc:mysql://localhost:3306/syos_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.username=root
db.password=SportS28

# Connection Pool Settings - UPDATED FOR WEB APPLICATION
# Previous: initial=5, max=10 (CLI only)
# Current: initial=20, max=100 (CLI + Web with 200 concurrent users)
db.pool.initial=20
db.pool.max=100

# Connection Pool Timeouts (milliseconds)
db.pool.connection.timeout=30000
db.pool.idle.timeout=600000
db.pool.max.lifetime=1800000

# Statement Settings
db.statement.timeout=30

# Logging
logging.level=INFO